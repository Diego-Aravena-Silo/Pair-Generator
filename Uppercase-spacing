# Get the current font and all its glyphs
font = Glyphs.font
glyphs = font.glyphs

# List of uppercase characters
uppercase = [chr(i) for i in range(65, 91)]

# Concatenate all results into a single string
result_string = "\n".join(["HH" + character + "HH" for character in uppercase])

# Create a new tab with the concatenated string
font.newTab(result_string)
